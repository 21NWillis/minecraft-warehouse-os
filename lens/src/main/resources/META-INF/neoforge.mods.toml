modLoader = "javafml"
loaderVersion = "[4,)"
license = "MIT"
issueTrackerURL = "https://github.com/21NWillis/minecraft-warehouse-os/issues"

[[mods]]
modId = "paperclip_lens"
version = "0.1.0"
displayName = "Paperclip Lens"
authors = "21NWillis, Claude"
# client-only: servers never see it, vanilla-compatible server list ping
displayTest = "IGNORE_ALL_VERSION"
description = '''
Client-side QoL for the Paperclip warehouse OS: EMC value tooltips (from the
warehouse's own emc.txt), live warehouse stock in tooltips via a published
snapshot, and an ESP overlay that outlines + labels ComputerCraft turtles.
Reads the world; changes nothing. No server install needed.
'''

[[dependencies.paperclip_lens]]
modId = "neoforge"
type = "required"
versionRange = "[21.1,)"
ordering = "NONE"
side = "CLIENT"

[[dependencies.paperclip_lens]]
modId = "minecraft"
type = "required"
versionRange = "[1.21.1,1.21.2)"
ordering = "NONE"
side = "CLIENT"
